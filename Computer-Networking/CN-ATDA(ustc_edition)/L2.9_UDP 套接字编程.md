# 2.9 UDP 套接字编程 —— 无连接的数据报通信

---

## 一、UDP 协议核心特性回顾

UDP（用户数据报协议）是传输层另一种重要的协议，与 TCP 形成鲜明对比。

### 1. UDP 的本质

|特性|描述|与 TCP 对比|
|---|---|---|
|**无连接**|通信前无需握手，直接发送数据|TCP 需要三次握手建立连接|
|**不可靠**|不保证数据送达，可能丢失、乱序、重复|TCP 提供可靠、保序的字节流|
|**面向报文**|**保留报文边界**，每个 `sendto()` 对应一个完整的报文，`recvfrom()` 一次读出一个完整的报文|TCP 是字节流，无报文边界，可能粘包|
|**轻量级**|首部仅 8 字节，无状态维护，开销小|TCP 首部 20-60 字节，需维护连接状态|

> 💡 **理解关键**：UDP 像“寄明信片”——每张明信片独立投递，不确认收件人是否收到，后寄的可能先到。而 TCP 像“打电话”——先接通，再通话，确保对方听清楚每一个字。

---

## 二、UDP 套接字编程模型

### 1. UDP 套接字特点

- **只需绑定本地端点**：UDP 套接字通过 `bind()` 绑定本地 IP 和端口，但**不绑定远程地址**。
    
- **无连接状态**：内核不为 UDP 维护连接表，一个套接字可与任意多个远端通信。
    
- **每次发送需指定目标**：每次调用 `sendto()` 都必须给出目标 IP 和端口。
    

### 2. UDP 服务器端流程

**关键点**：

- 一个 UDP 套接字可以服务所有客户端，无需为每个客户端创建新套接字。
    
- `recvfrom()` 不仅接收数据，还返回发送方的地址（IP + 端口），供 `sendto()` 回应用户。
    
- 无连接特性意味着服务器可随时响应任意客户端，无需维护会话状态。
    

---

### 3. UDP 客户端流程

**关键点**：

- 客户端一般不需要 `bind()`，系统会自动分配临时端口。
    
- `sendto()` 必须指定服务器地址。
    
- 可以**先后与不同服务器通信**，只需改变 `sendto()` 的目标地址即可。
    

---

## 三、核心 API 详解

### 1. `socket()` —— 创建 UDP 套接字

```c

int sockfd = socket(AF_INET, SOCK_DGRAM, 0);
```
- `AF_INET`：IPv4 协议族
    
- `SOCK_DGRAM`：数据报套接字（区别于 TCP 的 `SOCK_STREAM`）
    
- 返回值：套接字描述符（整数），失败返回 -1
    

---

### 2. `bind()` —— 绑定本地地址（可选）


```c
struct sockaddr_in localaddr;
localaddr.sin_family = AF_INET;
localaddr.sin_addr.s_addr = htonl(INADDR_ANY);  // 监听所有网络接口
localaddr.sin_port = htons(8888);               // 绑定端口
bind(sockfd, (struct sockaddr *)&localaddr, sizeof(localaddr));
```
- **服务器必须绑定知名端口**，以便客户端知道向哪里发送。
    
- **客户端可省略 bind**，系统自动分配临时端口。
    

---

### 3. `sendto()` —— 发送数据报

```c

ssize_t sendto(int sockfd, const void *buf, size_t len, int flags,
               const struct sockaddr *dest_addr, socklen_t addrlen);
```
- `dest_addr`：目标地址结构（IP + 端口）
    
- `addrlen`：地址结构长度
    
- 返回值：成功发送的字节数，失败 -1
    
- **每次调用必须指定目标地址**（即使连续发给同一个对端）
    

---

### 4. `recvfrom()` —— 接收数据报

```c

ssize_t recvfrom(int sockfd, void *buf, size_t len, int flags,
                 struct sockaddr *src_addr, socklen_t *addrlen);
```
- `src_addr`：**输出参数**，返回发送方的地址信息（IP + 端口）
    
- `addrlen`：**值-结果参数**，调用前需初始化为 `src_addr` 的长度，返回时表示实际填充的地址长度
    
- 返回值：成功接收的字节数，失败 -1
    

**重要**：通过 `src_addr` 获取客户端地址，用于后续响应。

---

### 5. `close()` —— 关闭套接字

```c

close(sockfd);
```
释放套接字占用的系统资源。

---

## 四、完整代码示例：UDP 大小写转换服务

### 1. UDP 服务器端（upperserver_udp.c）

	
```c

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <ctype.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#define SERVER_PORT 8888
#define BUFFER_SIZE 1024
int main() {
    int sockfd;
    struct sockaddr_in server_addr, client_addr;
    socklen_t client_len;
    char buffer[BUFFER_SIZE];
    int recv_len, i;
    // 1. 创建 UDP socket
    sockfd = socket(AF_INET, SOCK_DGRAM, 0);
    if (sockfd < 0) {
        perror("socket creation failed");
        exit(1);
    }
    // 2. 绑定本地地址
    memset(&server_addr, 0, sizeof(server_addr));
    server_addr.sin_family = AF_INET;
    server_addr.sin_addr.s_addr = htonl(INADDR_ANY);  // 监听所有接口
    server_addr.sin_port = htons(SERVER_PORT);
    if (bind(sockfd, (struct sockaddr *)&server_addr, sizeof(server_addr)) < 0) {
        perror("bind failed");
        close(sockfd);
        exit(1);
    }
    printf("UDP 服务器启动，监听端口 %d...\n", SERVER_PORT);
    while (1) {
        client_len = sizeof(client_addr);
        
        // 3. 接收数据报（阻塞）
        recv_len = recvfrom(sockfd, buffer, BUFFER_SIZE - 1, 0,
                           (struct sockaddr *)&client_addr, &client_len);
        if (recv_len < 0) {
            perror("recvfrom failed");
            continue;
        }
        buffer[recv_len] = '\0';  // 确保字符串结束
        printf("收到来自 %s:%d 的消息: %s\n",
               inet_ntoa(client_addr.sin_addr),
               ntohs(client_addr.sin_port),
               buffer);
        // 4. 转换为大写
        for (i = 0; i < recv_len; i++) {
            buffer[i] = toupper(buffer[i]);
        }
        // 5. 发回给客户端（使用相同的 client_addr）
        sendto(sockfd, buffer, recv_len, 0,
               (struct sockaddr *)&client_addr, client_len);
    }
    close(sockfd);
    return 0;
}
```
---

### 2. UDP 客户端（upperclient_udp.c）

```c

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <netdb.h>
#define SERVER_PORT 8888
#define BUFFER_SIZE 1024
int main(int argc, char *argv[]) {
    int sockfd;
    struct sockaddr_in server_addr;
    struct hostent *server;
    char send_buf[BUFFER_SIZE], recv_buf[BUFFER_SIZE];
    socklen_t addr_len;
    if (argc != 3) {
        fprintf(stderr, "用法: %s <服务器IP/域名> <消息>\n", argv[0]);
        exit(1);
    }
    // 1. 创建 UDP socket
    sockfd = socket(AF_INET, SOCK_DGRAM, 0);
    if (sockfd < 0) {
        perror("socket creation failed");
        exit(1);
    }
    // 2. 解析服务器地址
    memset(&server_addr, 0, sizeof(server_addr));
    server_addr.sin_family = AF_INET;
    server_addr.sin_port = htons(SERVER_PORT);
    // 判断是域名还是 IP 地址
    if (inet_pton(AF_INET, argv[1], &server_addr.sin_addr) <= 0) {
        // 不是点分十进制，尝试解析域名
        server = gethostbyname(argv[1]);
        if (server == NULL) {
            fprintf(stderr, "无法解析域名 %s\n", argv[1]);
            close(sockfd);
            exit(1);
        }
        memcpy(&server_addr.sin_addr, server->h_addr, server->h_length);
    }
    strcpy(send_buf, argv[2]);
    addr_len = sizeof(server_addr);
    // 3. 发送数据报给服务器
    sendto(sockfd, send_buf, strlen(send_buf), 0,
           (struct sockaddr *)&server_addr, addr_len);
    printf("已发送: %s\n", send_buf);
    // 4. 接收服务器的响应
    int recv_len = recvfrom(sockfd, recv_buf, BUFFER_SIZE - 1, 0,
                            (struct sockaddr *)&server_addr, &addr_len);
    if (recv_len < 0) {
        perror("recvfrom failed");
    } else {
        recv_buf[recv_len] = '\0';
        printf("服务器返回: %s\n", recv_buf);
    }
    close(sockfd);
    return 0;
}
```
---

## 五、UDP 与 TCP 套接字编程对比

|对比维度|UDP|TCP|
|---|---|---|
|**套接字类型**|`SOCK_DGRAM`|`SOCK_STREAM`|
|**连接建立**|无连接，直接发数据|需 `connect()` 三次握手|
|**地址绑定**|服务器必须 `bind()`；客户端可选|服务器必须 `bind()`；客户端可选|
|**数据边界**|**保留报文边界**，一次 `recvfrom()` 读一个完整报文|**无边界**，字节流，需应用层处理粘包|
|**通信对象**|一个套接字可与任意多个远端通信|一个连接套接字对应一个固定远端|
|**数据收发**|`sendto()`/`recvfrom()` 每次指定对方地址|`send()`/`recv()` 无需指定地址（已绑定连接）|
|**可靠性**|不可靠，需应用层处理丢包/乱序|可靠，内核保证不丢、不重、保序|
|**首部开销**|8 字节|20~60 字节|
|**典型应用**|DNS、DHCP、SNMP、RTP（音视频）、在线游戏、物联网传感器|HTTP、FTP、SMTP、SSH、数据库连接|

---

## 六、UDP 编程常见问题与陷阱

### 1. 数据报大小限制

- UDP 数据报长度理论最大 65507 字节（IPv4），但实际受 MTU 限制，通常建议不超过 1472 字节（以太网环境下，1500 - 20(IP头) - 8(UDP头)）。
    
- 超过 MTU 会导致 IP 分片，增加丢失风险。
    

### 2. 不可靠性处理

- 应用层需自行实现**超时重传**、**序列号**、**确认机制**（若需要可靠性）。
    
- 例如：DNS 查询若超时未收到响应，客户端会重发请求。
    

### 3. 端口未绑定问题

- 客户端若不 `bind()`，系统自动分配临时端口；若需固定端口（如 P2P 应用），需主动 `bind()`。
    

### 4. 多客户端并发

- UDP 服务器天然支持多客户端并发，无需多线程/多进程。但若处理逻辑耗时，可能丢失后续数据报，可考虑多线程并行处理。
    

### 5. 地址重用

- 与 TCP 类似，可用 `setsockopt()` 设置 `SO_REUSEADDR`，允许快速重启服务。
    

---

## 七、知识小结

|知识点|核心内容|重点/易混淆点|难度|
|---|---|---|---|
|**UDP 协议特性**|无连接、不可靠、面向报文|与 TCP 的**三大本质区别**|★★★|
|**UDP 套接字创建**|`socket(AF_INET, SOCK_DGRAM, 0)`|参数 `SOCK_DGRAM`|★★|
|**服务器流程**|`socket()` → `bind()` → `recvfrom()`/`sendto()`|无需 `listen()`/`accept()`，一个套接字服务所有客户端|★★★★|
|**客户端流程**|`socket()` → （可选 `bind()`） → `sendto()`/`recvfrom()`|无需 `connect()`，可动态改变通信对象|★★★★|
|**核心 API**|`sendto()`（需指定目标）、`recvfrom()`（获来源地址）|`recvfrom()` 的地址参数是**输出参数**|★★★|
|**报文边界**|每个 `sendto()` 对应一个完整报文，`recvfrom()` 一次读出一个|与 TCP 字节流的**最关键区别**|★★★★★|
|**典型应用**|DNS、DHCP、SNMP、RTP、在线游戏、物联网|理解为何这些应用选择 UDP 而非 TCP|★★★|
|**可靠性处理**|若需可靠性，应用层自行实现|确认、重传、序号、去重|★★★★★|

---

## 八、总结

UDP 套接字编程相比 TCP 更为简单直接，但同时也将可靠性、流量控制等复杂问题抛给了应用层开发者。选择 UDP 还是 TCP，取决于应用场景：

- 若需要**可靠、有序、面向连接**的通信，如文件传输、网页浏览 → **选择 TCP**
    
- 若追求**低延迟、容忍少量丢包、支持广播/多播**，如实时音视频、在线游戏、物联网传感器数据 → **选择 UDP**
    

掌握 UDP 编程，意味着你能够驾驭互联网中那些“快”而“灵活”的应用场景，为后续学习多媒体协议、实时通信、分布式系统打下基础。