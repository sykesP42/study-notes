# 📚 第15讲：不相交集合的渐进分析与优化

## 一、不相交集合（Disjoint Sets）深度回顾

### 📋 API 再认识

不相交集合数据结构提供两个核心操作：

1. **isConnected(p, q)** - 检查两个元素是否连通
    
2. **connect(p, q)** - 连接两个元素
    


```java

public interface DisjointSets {
    void connect(int p, int q);
    boolean isConnected(int p, int q);
}
```
### 🔄 实现方式的演进历程

#### 1. **ListOfSetsDS（集合列表法）**

```java

// 使用List<Set<Integer>>存储连通分量
public class ListOfSetsDS implements DisjointSets {
    private List<Set<Integer>> components;
    
    // 初始化：每个元素单独成集合
    public ListOfSetsDS(int N) {
        components = new ArrayList<>();
        for (int i = 0; i < N; i++) {
            Set<Integer> set = new HashSet<>();
            set.add(i);
            components.add(set);
        }
    }
    
    @Override
    public boolean isConnected(int p, int q) {
        // 需要遍历所有集合找到p和q
        Set<Integer> setP = null, setQ = null;
        for (Set<Integer> set : components) {
            if (set.contains(p)) setP = set;
            if (set.contains(q)) setQ = set;
        }
        return setP == setQ; // 比较是否为同一集合
    }
    
    @Override
    public void connect(int p, int q) {
        // 找到包含p和q的集合，合并它们
        Set<Integer> setP = null, setQ = null;
        for (Set<Integer> set : components) {
            if (set.contains(p)) setP = set;
            if (set.contains(q)) setQ = set;
        }
        
        if (setP != setQ) {
            setP.addAll(setQ);      // 合并集合
            components.remove(setQ); // 移除被合并的集合
        }
    }
}
```
**性能分析**：

- **构造器**：Θ(N) - 创建N个单元素集合
    
- **isConnected**：O(N) - 最坏需要遍历所有集合
    
- **connect**：O(N) - 需要查找两个集合并合并
    

**问题**：随着元素增加，集合查找成为瓶颈。

#### 2. **QuickFindDS（快速查找法）**

```java

public class QuickFindDS implements DisjointSets {
    private int[] id; // id[i] = 元素i的集合编号
    
    public QuickFindDS(int N) {
        id = new int[N];
        for (int i = 0; i < N; i++) {
            id[i] = i; // 每个元素初始独立
        }
    }
    
    @Override
    public boolean isConnected(int p, int q) {
        return id[p] == id[q]; // O(1)!
    }
    
    @Override
    public void connect(int p, int q) {
        int pid = id[p];
        int qid = id[q];
        if (pid == qid) return;
        
        // 将所有属于pid集合的元素改为qid
        for (int i = 0; i < id.length; i++) {
            if (id[i] == pid) {
                id[i] = qid; // 需要修改大量元素
            }
        }
    }
}
```
**性能分析**：

|操作|时间复杂度|解释|
|---|---|---|
|构造器|Θ(N)|初始化数组|
|**isConnected**|**Θ(1)**|仅需比较两个数组元素|
|**connect**|**Θ(N)**|需要遍历整个数组修改值|

**关键缺陷**：连接操作太慢，每次可能需要修改大量元素。

#### 3. **QuickUnionDS（快速合并法）**

```java

public class QuickUnionDS implements DisjointSets {
    private int[] parent; // parent[i] = 元素i的父节点
    
    public QuickUnionDS(int N) {
        parent = new int[N];
        for (int i = 0; i < N; i++) {
            parent[i] = -1; // -1表示根节点
        }
    }
    
    // 查找元素i的根节点
    private int find(int i) {
        while (parent[i] >= 0) {
            i = parent[i]; // 向上查找
        }
        return i;
    }
    
    @Override
    public boolean isConnected(int p, int q) {
        return find(p) == find(q);
    }
    
    @Override
    public void connect(int p, int q) {
        int rootP = find(p);
        int rootQ = find(q);
        if (rootP != rootQ) {
            parent[rootP] = rootQ; // 简单地将一个根指向另一个根
        }
    }
}
```
**性能分析**：

- **find操作**：O(H)，H是树的高度
    
- **最坏情况**：树退化为链表，高度H = N
    
- **connect/isConnected**：最坏O(N)
    

**退化场景示例**：

```text

连续执行：
connect(4, 3)  // 树：4←3
connect(3, 2)  // 树：4←3←2
connect(2, 1)  // 树：4←3←2←1
connect(1, 0)  // 树：4←3←2←1←0

此时树高=4，如果有N个元素，最坏树高=N。
```
## 二、关键优化：加权快速合并（Weighted Quick Union）

### 💡 优化思想

合并时总是将**较小的树**连接到**较大的树**下，防止树过高。

### 📊 数据结构设计

```java

public class WeightedQuickUnionDS implements DisjointSets {
    private int[] parent; // 根节点存储负的树大小
    
    public WeightedQuickUnionDS(int N) {
        parent = new int[N];
        for (int i = 0; i < N; i++) {
            parent[i] = -1; // 初始大小=1
        }
    }
    
    private int find(int i) {
        while (parent[i] >= 0) {
            i = parent[i];
        }
        return i;
    }
    
    @Override
    public boolean isConnected(int p, int q) {
        return find(p) == find(q);
    }
    
    @Override
    public void connect(int p, int q) {
        int rootP = find(p);
        int rootQ = find(q);
        
        if (rootP == rootQ) return;
        
        // 比较树的大小（注意：根节点存储负值）
        int sizeP = -parent[rootP];
        int sizeQ = -parent[rootQ];
        
        // 总是将较小的树连接到较大的树下
        if (sizeP < sizeQ) {
            parent[rootP] = rootQ;           // 小树根指向大树根
            parent[rootQ] = -(sizeP + sizeQ); // 更新大树大小
        } else {
            parent[rootQ] = rootP;           // 小树根指向大树根
            parent[rootP] = -(sizeP + sizeQ); // 更新大树大小
        }
    }
}
```
### 🧮 数学分析：为什么树高是O(log N)

**定理**：使用加权快速合并，包含k个节点的树的高度最多为⌊log₂k⌋。

**证明思路**：

1. 考虑节点x所在树的高度h(x)
    
2. 当x的父节点改变时（合并操作），x所在树的大小至少翻倍
    
3. 树的大小最多可以翻倍log₂N次就达到N
    
4. 因此高度最多为log₂N
    

**直观示例**：

```text

最坏情况下的构建过程：
1. 合并两个大小为1的树 → 大小=2，高度=1
2. 合并两个大小为2的树 → 大小=4，高度=2
3. 合并两个大小为4的树 → 大小=8，高度=3
4. 合并两个大小为8的树 → 大小=16，高度=4
```
规律：
$$
树高 = log₂(树的大小)
$$
### 📈 性能对比表

|实现方式|构造器|connect|isConnected|空间|
|---|---|---|---|---|
|ListOfSetsDS|Θ(N)|O(N)|O(N)|O(N)|
|QuickFindDS|Θ(N)|**Θ(N)**|**Θ(1)**|O(N)|
|QuickUnionDS|Θ(N)|O(N)|O(N)|O(N)|
|**WeightedQuickUnionDS**|Θ(N)|**O(log N)**|**O(log N)**|O(N)|

## 三、终极优化：路径压缩（Path Compression）

### 🚀 优化思想

在查找根节点过程中，将路径上的所有节点直接指向根节点，压平树结构。

### 🔧 实现方式

```java

// 递归版本（简洁但可能栈溢出）
private int findWithCompression(int i) {
    if (parent[i] < 0) {
        return i; // 根节点
    } else {
        // 递归查找并压缩路径
        parent[i] = findWithCompression(parent[i]);
        return parent[i];
    }
}

// 迭代版本（推荐，避免栈溢出）
private int findWithCompressionIterative(int i) {
    // 第一步：找到根节点
    int root = i;
    while (parent[root] >= 0) {
        root = parent[root];
    }
    
    // 第二步：压缩路径
    while (i != root) {
        int next = parent[i];
        parent[i] = root;
        i = next;
    }
    
    return root;
}
```
### 🎯 压缩效果可视化

```text

压缩前：          压缩后：
    0                 0
   / \              /|\
  1   2            1 2 3
      |
      3
```
执行 find(3) 后，将3直接连接到根节点0

### 🔄 反直觉特性

1. **越用越快**：数据结构使用越频繁，效率越高
    
2. **自优化**：每次操作都会优化结构，减少后续操作时间
    
3. **极限扁平**：经过足够多次操作，树将几乎完全扁平化
    

## 四、渐进分析（Asymptotic Analysis）

### 📊 复杂度函数直观理解

|复杂度类别|N翻倍的影响|N加1的影响|典型例子|
|---|---|---|---|
|**Θ(1)**|不影响运行时间|不影响运行时间|数组索引访问|
|**Θ(log N)**|运行时间+1|极小影响|二分查找|
|**Θ(N)**|运行时间×2|运行时间+1|遍历数组|
|**Θ(N²)**|运行时间×4|运行时间+~N|嵌套循环|
|**Θ(2ᴺ)**|运行时间²|运行时间×2|穷举搜索|

### 🔢 迭代对数函数 log* N

**定义**：log* N 表示需要对N连续取多少次以2为底的对数，结果才会≤1。

**计算示例**：

```text

log* 1 = 0          (1 ≤ 1)
log* 2 = 1          (log₂2 = 1 ≤ 1)
log* 4 = 2          (log₂4 = 2, log₂2 = 1)
log* 16 = 3         (log₂16 = 4, log₂4 = 2, log₂2 = 1)
log* 65536 = 4      (65536 → 16 → 4 → 2 → 1)
log* 2⁶⁵⁵³⁶ = 5     (2⁶⁵⁵³⁶ → 65536 → 16 → 4 → 2 → 1)
```
**关键性质**：对于任何实际应用的N，log* N ≤ 5！

### ∞ 逆阿克曼函数 α(N)

**定义**：α(N)是阿克曼函数的反函数，增长极其缓慢。

**阿克曼函数定义**：

```text

A(1, j) = 2ʲ          (j ≥ 1)
A(i, 1) = A(i-1, 2)   (i ≥ 2)
A(i, j) = A(i-1, A(i, j-1))  (i, j ≥ 2)
```
**逆阿克曼函数值**：

```text

α(3) = 1
α(7) = 2
α(63) = 3
α(2^2^...^2) [共65536个2] = 4
α(巨大的数) = 5
```
**实际意义**：对于任何实际应用的N，α(N) ≤ 4！

## 五、并查集的最终性能

### 📈 性能总结表

|实现方式|M次操作总时间复杂度|单次操作摊销复杂度|适用场景|
|---|---|---|---|
|ListOfSetsDS|O(NM)|O(N)|仅用于教学|
|QuickFindDS|O(NM)|O(N)|查询远多于合并|
|QuickUnionDS|O(NM)|O(N)|基础版本|
|WeightedQuickUnionDS|O(M log N)|O(log N)|通用场景|
|**WeightedQuickUnionWithPathCompression**|**O(M α(N))**|**O(α(N))**|**最优选择**|

### 🎯 实际性能对比

假设N=10⁹（十亿）个元素：

|实现方式|M=10⁹次操作所需时间|对比|
|---|---|---|
|QuickFindDS|~30年|无法接受|
|QuickUnionDS|~30年|无法接受|
|WeightedQuickUnionDS|~6秒|优秀|
|WQU with Path Compression|~0.6秒|极佳|

### 🔬 摊销分析（Amortized Analysis）

**定义**：虽然单次操作可能较慢，但多次操作的平均时间很好。

**并查集的摊销分析**：

- **最坏情况单次操作**：O(log N)
    
- **M次操作摊销时间**：O(α(N)) ≈ O(1)
    

**类比ArrayList的摊销分析**：

```java

// ArrayList的addLast操作
public void addLast(T item) {
    if (size == items.length) {
        resize(size * 2); // 几何扩容：O(N)
    }
    items[size] = item;
    size++;
}
```
- **单次resize**：O(N)
    
- **N次addLast的摊销时间**：O(1)
    

## 六、算法设计过程总结

### 🎨 设计迭代历程

1. **问题抽象**：动态连通性问题
    
2. **朴素实现**：ListOfSetsDS - O(N)操作
    
3. **第一次优化**：QuickFindDS - O(1)查询，O(N)合并
    
4. **结构优化**：QuickUnionDS - 树形结构，但可能退化
    
5. **平衡优化**：WeightedQuickUnionDS - O(log N)操作
    
6. **终极优化**：路径压缩 - O(α(N))摊销时间
    

### 💡 关键设计原则

1. **表示法选择**：用连通分量而非连接边
    
2. **数据结构演进**：列表 → 数组 → 树 → 平衡树 → 压缩树
    
3. **渐进优化**：关注最坏情况，寻求理论保证
    
4. **摊销分析**：实际性能优于最坏情况分析
    

## 七、渐进分析技巧

### 🔍 循环分析示例

#### 示例1：双重循环（三角形求和）

```java

public static void dup1(int N) {
    for (int i = 0; i < N; i++) {
        for (int j = i + 1; j < N; j++) {
            System.out.println("hello");
        }
    }
}
```
**分析**：

- 外层循环：i从0到N-1
    
- 内层循环：j从i+1到N-1
    
- 总操作数：1 + 2 + 3 + ... + (N-1) = N(N-1)/2
    
- **时间复杂度**：Θ(N²)
    

#### 示例2：几何增长循环

```java

public static void printParty(int n) {
    for (int i = 1; i <= n; i = i * 2) {
        for (int j = 0; j < i; j++) {
            System.out.println("hello");
        }
    }
}
```
**分析**：

- 外层循环：i = 1, 2, 4, 8, ..., ≤ n
    
- 循环次数：log₂n次
    
- 内层操作总数：1 + 2 + 4 + ... + n = 2n - 1
    
- **时间复杂度**：Θ(n)
    

### 📚 常用求和公式

1. **等差数列求和**：1 + 2 + 3 + ... + Q = Q(Q+1)/2 = Θ(Q²)
    
2. **幂次求和**：1ᵏ + 2ᵏ + ... + Qᵏ = Θ(Qᵏ⁺¹)
    
3. **几何级数求和**：1 + r + r² + ... + rᴺ = (rᴺ⁺¹ - 1)/(r - 1) = Θ(rᴺ) (r > 1)
    
4. **特殊几何级数**：1 + 2 + 4 + ... + 2ᴺ = 2ᴺ⁺¹ - 1 = Θ(2ᴺ)
    

## 八、递归算法分析

### 🔄 递归示例

```java

public static int f3(int n) {
    if (n <= 1) {
        return 1;
    }
    return f3(n - 1) + f3(n - 1);
}
```
**递归树分析**：

```text

          f3(n)
         /     \
    f3(n-1)   f3(n-1)
    /    \     /    \
f3(n-2) f3(n-2) ... 
```
**时间复杂度**：

- 每层调用数：1, 2, 4, 8, ..., 2ⁿ
    
- 总调用数：1 + 2 + 4 + ... + 2ⁿ = 2ⁿ⁺¹ - 1
    
- **时间复杂度**：Θ(2ⁿ)
    

## 九、知识体系总结

### 🗺️ 并查集知识地图

```text

并查集 (Disjoint Sets)
├── 问题定义：动态连通性
├── API设计：connect, isConnected
├── 实现演进
│   ├── ListOfSetsDS (O(N))
│   ├── QuickFindDS (查O(1), 连O(N))
│   ├── QuickUnionDS (可能O(N))
│   ├── WeightedQuickUnionDS (O(log N))
│   └── WQU with Path Compression (O(α(N)))
├── 优化技术
│   ├── 加权合并 (平衡树高)
│   └── 路径压缩 (扁平化树)
└── 分析工具
    ├── 渐进分析 (Big-O/Θ/Ω)
    ├── 摊销分析
    └── 递归分析
```
### 🎯 核心要点

1. **渐进分析是基础**：理解不同复杂度类别的含义
    
2. **摊销分析更实用**：反映实际使用性能
    
3. **算法设计是迭代过程**：从简单开始，逐步优化
    
4. **理论指导实践**：数学分析确保性能边界
    

### 🚀 学习建议

1. **动手实现**：亲自编写每个版本的并查集
    
2. **性能测试**：对比不同实现的运行时间
    
3. **可视化理解**：使用工具观察树结构变化
    
4. **应用实践**：在算法题中应用并查集（如Kruskal算法）
    

## 十、扩展学习

### 📖 推荐资源

1. **经典论文**：Robert Tarjan的《Efficiency of a Good But Not Linear Set Union Algorithm》
    
2. **算法教材**：《算法导论》第21章
    
3. **可视化工具**：VisuAlgo的并查集演示
    
4. **竞赛应用**：LeetCode中并查集相关题目
    

### 🔬 研究方向

1. **并行并查集**：多线程环境下的并查集算法
    
2. **持久化并查集**：支持历史版本查询
    
3. **动态并查集**：支持动态添加/删除元素
    
4. **应用扩展**：在图算法、编译优化、数据库等领域的应用